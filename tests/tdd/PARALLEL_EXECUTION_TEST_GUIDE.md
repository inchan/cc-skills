# 병렬 실행 기능 테스트 가이드 (압축판)

> 다음 세션에서 바로 실행 가능

## 📋 변경 사항 요약

### 1. 구현 완료 (2025-11-29)
- **파일**: `agents/tdd/orchestrator.md` (v1.0 → v2.0)
- **추가**: 병렬 실행 기능 (~230줄)
- **성능**: 20개 독립 작업 기준 4배 빠름 (300분 → 75분)

### 2. 핵심 기능
```
✅ 의존성 자동 분석 (dependencies 필드 활용)
✅ 파일 충돌 검증 (2단계 방어)
✅ 최대 4개 동시 실행
✅ 배치 부분 실패 시 즉시 사용자 질문
```

---

## 🚀 빠른 테스트 실행

### Test 11: 배치 그룹화 로직 (5분)

**목적**: 알고리즘 검증 (수동 시뮬레이션)

**명령어**:
```bash
# 1. orchestrator.md 확인
cat agents/tdd/orchestrator.md | sed -n '/2.0 작업 배치 그룹화/,/2.1 배치별 병렬 실행/p' | head -50

# 2. 테스트 가이드 읽기
cat agents/tdd/tests/test-11-parallel-batch-grouping.md
```

**검증 시나리오**:
```
시나리오 1: 독립 4개 → Batch 1개 (4개 병렬) ✓
시나리오 2: 체인 의존성 → Batch 3개 (순차) ✓
시나리오 3: 파일 충돌 → Batch 2개 (충돌 분리) ✓
시나리오 4: 혼합 7개 → Batch 2개 (최적화) ✓
```

**성공 기준**: 4개 시나리오 예상 일치

---

### Test 12: 전체 워크플로우 (10분)

**목적**: 실제 `/tdd-team` 실행 검증

**환경 준비**:
```bash
mkdir -p /tmp/claude/tdd-test-12
cd /tmp/claude/tdd-test-12

echo '{"name":"test-12","scripts":{"test":"jest"},"devDependencies":{"jest":"^29.0.0"}}' > package.json

git init
git config user.name "Test User"
git config user.email "test@example.com"
```

**실행**:
```bash
# Claude Code에서 실행
/tdd-team "4개의 독립적인 수학 함수를 만들어주세요: sum, multiply, divide, subtract"
```

**확인 사항**:
```bash
# 1. TodoWrite 확인
# 예상: "1개 배치", "TASK-001, TASK-002, TASK-003, TASK-004 (병렬)"

# 2. 파일 확인
ls src/
# 예상: sum.js, sum.test.js, multiply.js, multiply.test.js, ...

# 3. 테스트
npm test
# 예상: 4 passed
```

**성공 기준**: Batch 1개 + 파일 8개 + 테스트 4/4

---

## 📊 테스트 체크리스트

```
[ ] Test 11: 배치 그룹화 시뮬레이션 (5분)
    [ ] 시나리오 1-4 모두 예상과 일치
    [ ] orchestrator.md 섹션 2.0 존재 확인

[ ] Test 12: 전체 워크플로우 (10분)
    [ ] /tdd-team 실행
    [ ] Batch 1개로 그룹화
    [ ] 8개 파일 생성
    [ ] 4개 테스트 통과

[ ] 문서 확인
    [ ] orchestrator.md 변경 이력 확인
    [ ] 버전 2.0 표시 확인
```

---

## 🔧 문제 해결

### 문제 1: Batch 4개로 분할 (병렬 안 됨)
**원인**: orchestrator.md v1.0 사용 중
**해결**:
```bash
# 버전 확인
tail -5 agents/tdd/orchestrator.md | grep "Version"
# 예상: Version: 2.0
```

### 문제 2: 파일 미생성
**원인**: 서브에이전트 제약 (알려진 이슈)
**해결**: Test 11 로직 검증으로 대체

### 문제 3: 파일 충돌 발생
**원인**: Task Planner가 같은 파일 경로 생성
**해결**: 프롬프트에 "각각 독립적인 파일" 명시

---

## 📁 관련 파일

```
agents/tdd/
├── orchestrator.md (✏️ 수정, v2.0)
└── tests/
    ├── test-11-parallel-batch-grouping.md (✨ 신규)
    ├── test-12-parallel-execution.md (✨ 신규)
    └── PARALLEL_EXECUTION_TEST_GUIDE.md (이 파일)
```

---

## 🎯 다음 세션 시작 시

1. **이 파일 읽기**
   ```bash
   cat agents/tdd/tests/PARALLEL_EXECUTION_TEST_GUIDE.md
   ```

2. **Test 11 실행** (5분)
   - 수동 시뮬레이션
   - 알고리즘 검증

3. **Test 12 실행** (10분)
   - /tdd-team 명령어
   - 실제 병렬 실행 확인

4. **결과 리포트**
   - 성공/실패 기록
   - agents/tdd/tests/README.md 업데이트

---

## 💡 핵심 개념 (복습)

### 배치 그룹화 알고리즘
```
1. 의존성 충족 확인 (completed_tasks에 모든 dependencies 있는지)
2. 파일 충돌 검사 (file_map에 중복 경로 없는지)
3. 최대 4개 선택
4. 다음 배치로 진행
```

### 예상 성능
```
독립 4개: 1 배치 × 15분 = 15분 (순차 60분 대비 4배)
독립 10개: 3 배치 × 15분 = 45분 (순차 150분 대비 3.3배)
혼합 20개: 5 배치 × 15분 = 75분 (순차 300분 대비 4배)
```

---

**다음 세션에서 만나요!** 🚀
